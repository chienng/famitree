services:
  famitree:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - famitree-data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3000
      - NODE_ENV=production

volumes:
  famitree-data:
